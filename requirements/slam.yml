name: slam-dev

channels:
  - conda-forge

dependencies:

# setup dependencies
  - setuptools >=61
  - setuptools-scm >=7

# core dependencies
  - geovista >=0.2
  - iris >=3.2
  - numpy >=1.23

# dev dependencies
  - pip
  - pre-commit

# test dependencies
  - codecov
  - pytest >=6.0
  - pytest-cov
