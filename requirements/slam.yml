name: slam-dev

channels:
  - conda-forge

dependencies:

# setup dependencies
  - setuptools >=64
  - setuptools-scm >=8

# core dependencies
  - geovista >=0.2
  - iris >=3.2
  - numpy >=1.23

# dev dependencies
  - pip
  - pre-commit

# test dependencies
  - codecov
  - pytest >=6.0
  - pytest-cov
