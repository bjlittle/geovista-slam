name: slam-dev

channels:
  - conda-forge

dependencies:

# setup dependencies
  - setuptools >=61
  - setuptools-scm >=7

# core dependencies
  - appdirs
  - geovista
  - iris

# dev dependencies
  - pip
  - pre-commit

# test dependencies
  - codecov
  - pytest >=6.0
  - pytest-cov
